spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: Rahasia123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  flyway:
    enabled: true
    clean-disabled: false

  # Mail properties for testing
  mail:
    host: localhost
    port: 3025
    username: test
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
